const age = 26;
const dogYears = (age + 1) / 7;

// Print out the dogeYears
console.log("num", dogYears);

// Challenge area
const studentScore = 18;
const maxScore = 20;

const percent = (studentScore / maxScore) * 100;

// Print out the percent;
console.log("challenge", percent);
