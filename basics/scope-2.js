// Global scope(name)
     // Local scope(name)
         // Local scope(name)
     // Local scope



const name = "Divine";

const isName = true;


if(isName) {
     let name = "Love"

  if(isName) {
    name = "Success"
    console.log("name", name)
  }

}



if(isName) {
    console.log("secondName ", name)
}